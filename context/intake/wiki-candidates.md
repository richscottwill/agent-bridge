# Wiki Candidates Queue

Append-only log of wiki article candidates and enrichment opportunities observed by hooks during their normal runs. Hooks observe — they do not analyze, classify, or draft. The wiki-editor consumes this file during the weekly `wiki-maintenance` manual hook run and proposes action.

## Format

One line per candidate. Loose format — hooks don't judge quality or priority. That's the editor's job.

```
[YYYY-MM-DDTHH:MM hook-name] signal: <one-line observation> | source: <where it came from> | proposed: <article slug or "enrich: existing-slug">
```

## Rules for contributing hooks

- One line per candidate. Do not expand.
- Timestamp in PT (use `TZ=America/Los_Angeles date +%Y-%m-%dT%H:%M`).
- Hook name = your hook's short name (am-auto, eod, session-summary, wbr-callouts).
- `signal:` — what you observed in one sentence.
- `source:` — DuckDB table, file path, Slack thread ID, meeting session ID.
- `proposed:` — either a new article slug OR `enrich: <existing-slug>` if the signal updates an existing doc.
- If unsure whether something is wiki-worthy, err on the side of appending. Noise is cheaper than misses.
- Do not deduplicate against existing entries. The consumer handles dedup.

## Rules for consuming hook (wiki-maintenance)

- Read this file during the manual wiki-maintenance run.
- Dedupe by signal text similarity + proposed slug.
- Group related signals (multiple hooks flagged the same topic = stronger signal).
- Propose top 3-5 items to `_meta/review-queue.md` for Richard's review.
- Archive processed entries by moving them to `archive/wiki-candidates-YYYY-WNN.md`.
- Do not draft articles from this file. Only propose.

## Kill condition

**2026-06-01.** If this queue accumulates >100 unprocessed entries AND wiki-editor hasn't promoted any to review-queue.md, the contribution pattern is noise — delete the file, revert the hook contributions, and go back to manual article creation. Keep-or-kill on 6/1.

---

## Candidates


<!-- Hooks append below this line -->
<!-- Last archive: 2026-W16 second pass (2026-04-17 19:00 PT). Prior entries moved to archive/wiki-candidates-2026-W16.md. -->
<!-- Last archive: 2026-W18 (2026-05-01). 362 entries moved to archive/wiki-candidates-2026-W18.md. -->
<!-- Last archive: 2026-W18 second pass (2026-05-01 19:00 PT). 5 pipeline-meta entries archived; no topic promotions. -->
[2026-05-01T15:59 session-summary] signal: Weekly maintenance hooks need an idempotency check before Half A executes — same-day re-invocation should detect prior-run artifacts (state-file mtimes, review file mtimes, cache timestamps) and skip-report rather than produce duplicate enrichments or re-grade completed DRAFTs; session-log scan works but is fragile as the sole tripwire. | source: this-session | proposed: enrich: wiki-pipeline-rules
[2026-05-01T16:29 session-summary] signal: Stage-level idempotency beats run-level idempotency for weekly maintenance hooks — re-running the same hook the same day still produces throughput if each stage independently checks "has my input changed since last fire?" rather than the orchestrator checking "has this hook run today?"; morning noop'd Stage 4 ran cleanly in the evening by picking an executable item from the same queue. | source: this-session | proposed: enrich: wiki-pipeline-rules
[2026-05-01T16:29 session-summary] signal: Blind dual-eval protocol (pipeline-rules §1) produces split verdicts on borderline articles where rubric-mode approves but reader-simulation finds packaging issues — the split IS the signal, not noise; articles that score 7-8 on rubric but 7 on reader-sim belong in revision-queue not FINAL, and the reader-persona catches audience-specific concerns (Kate's wall-of-prose scanning pattern, Lena's practical follow-up questions) the rubric structurally can't see. | source: this-session | proposed: enrich: wiki-pipeline-rules
[2026-05-01T16:36 session-summary] signal: "Richard-blocked" status in pipeline tracking should require a concrete clarifying question in one sentence, not fuzzy "needs Richard input somehow" — three of four blockers surfaced in the W18 re-run failed this test (stale cached values, premature decomposition ask, orphaned ops flag reference); anything that can't pass the one-sentence-question test belongs in process-observations, not pending-decisions. | source: this-session | proposed: enrich: wiki-pipeline-rules
[2026-05-01T16:36 session-summary] signal: Wiki articles that reference canonical numbers (CCP values, OP2 targets, rate card) should cite the source-of-truth location (column U of CCP Q1'26 check yc.xlsx + ps.market_constraints_manual), not bake in specific values — the source-of-truth pattern decouples article freshness from rate-card negotiation cadence and prevents the "pre-negotiation value gets enshrined as canonical" failure mode that happened with MX $90/$30 getting propagated across the W18 queue for 10 days before correction. | source: this-session | proposed: enrich: ieccp-planning-framework
[2026-05-01T20:16 session-summary] signal: "no, don't just hand it off with a bus post because you think it's too big" — when an agent has override authority for a single-shot completion session, declining items with reasoning that rationalizes not trying (pipeline-work / UX-reshape / too-large-scope) is a failure mode; the fix is to re-examine each declined item for what's the narrowest-useful version, not to punt | source: this-session | proposed: enrich: existing-slug agent-load-shape-discipline

[2026-05-01T21:30 session-summary] signal: "env-gated" skip labels are first-draft judgments — always re-examine before closing a shipping window to find narrower-but-honest scope cuts; 4 of 6 env-gated items in this session turned out to be shippable once the LLM/integration framing was dropped. | source: this-session | proposed: enrich: agent-load-shape-discipline
[2026-05-03T13:00 session-summary] signal: Kiro hook-schema drift — non-canonical fields (workspaceFolderName, shortName) cause the loader to misclassify a hook as a spec; diff against working siblings is the fastest diagnostic. | source: this-session | proposed: kiro-hook-schema-troubleshooting
[2026-05-03T06:15 session-summary] signal: Internal-vs-external infra decisions for small-scale agent workloads should start with "who already runs this for my team?" (ABMA sandbox + QuickSuite) before scoping a build — the replace-external framing is usually wrong; the real leverage is teammate self-serve, not eliminating the external dep. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T06:17 session-summary] signal: Before recommending a ticket/access-request, enumerate the user's existing LDAP/team memberships — "what can you already do today?" is a materially different question from "what would you need access for?" and the first-draft answer was wrong because I skipped the enumeration step. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T13:25 session-summary] signal: DevSpaces container can't reach ABDAI Redshift VPC via TCP — the `ada + DuckDB` path works for auth but not network, meaning agent-side hooks must stay on MotherDuck even when teammate-facing surfaces move to Redshift; this is a correctness boundary not a weakness, and the migration decision split is "agents = MotherDuck, humans = Hubble/QuickSuite" rather than "everything moves." | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T13:45 session-summary] signal: Asana registry stale-GID detection (AU/MX archived projects returning 424) should be a Phase 0 check in am-backend, not a mid-Phase-1 failure — schema drift handling protocol needs proactive probe before execution | source: this-session | proposed: enrich: asana-schema-drift-handling (likely new slug, no existing article)
[2026-05-03T22:45 session-summary] signal: Redshift client-side architecture — "postgres wire protocol + IAM-minted password" (what DuckDB postgres extension does) looks like it should work but fails BDT Shepherd audit because it doesn't federate SDO identity; the correct path is Redshift JDBC driver with groupFederation=true, OR Redshift Data API for programmatic clients. This pattern matters because it's easy to stand up the wrong thing and get a Shepherd ticket 10 days later. | source: this-session | proposed: new: redshift-client-path-selection
[2026-05-03T16:55 session-summary] signal: ABDAI `Redshift_Person_SDO_Identity_Access_Role` has ONLY `redshift:GetClusterCredentialsWithIAM` — not `GetClusterCredentials` (no WithIAM) and not `redshift-data:ExecuteStatement` — which means only specific IAM-federated JDBC paths work; Data API, default `redshift_connector(iam=True)`, and older postgres-style IAM clients all AccessDenied. This is the canonical compatibility matrix any future agent/person integrating with ABDAI Redshift needs up front. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-04T00:30 session-summary] signal: "Ready-to-file ticket draft kept dormant until concrete pull" is a cleaner default than either filing speculatively or throwing the draft away — the artifact survives in a bus thread as retrievable capacity without costing another team a review cycle; works because the cost of preserving a draft is ~0 and the cost of filing something nobody needs is non-zero. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T14:55 session-summary] signal: Team-facing "how to query our data" documents should seed with pattern-inferred queries + an explicit "first reader validates and updates" clause rather than waiting to verify every table name — the doc's purpose is to give teammates a starting point, not be comprehensive on publish, and the self-correcting-on-first-use pattern keeps it alive. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T15:30 session-summary] signal: When an agent can't directly verify a schema/API/dataset, the correct artifact to publish is a template with explicit `<placeholder>` markers + a "run this enumeration query first" instruction block at the top — NOT pattern-inferred content dressed up to look authoritative; the published doc looked credible enough that the first-query failure felt like a gotcha instead of the expected output of a deliberate "publish template, validate on use" approach. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T15:10 session-summary] signal: Team-facing query libraries should tier by complexity (simple single-table → CTEs+windows → multi-table specialized joins) so new teammates and senior analysts both find their entry point in the same doc — the "eight simple queries first, then complexity escalates" shape beats either a pure beginner doc or a pure reference. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T15:15 session-summary] signal: Parallel tool calls that combine fsWrite + MCP upload-from-path can race — the upload fires before the local file exists and ships an empty body — so any "build local file then upload" sequence must be sequential, OR use the MCP's inline content parameter instead of sourcePath. Caught this time via a size-0 readback but would have silently overwritten a live team doc with empty content otherwise. | source: this-session | proposed: enrich: tool-use-hygiene-notes
[2026-05-03T17:30 session-summary] signal: When parsing structured strings in SQL (reftags from URLs, IDs from composites, etc.), reach for SPLIT_PART before REGEXP_SUBSTR — the string function has no engine-flavor concerns, no quantifier edge cases, handles most real-world cases with two nested calls, and Redshift's regex engine has known quirks (silent failures, version-specific POSIX behavior) that pure string ops don't. Caught this after a regex-based URL-reftag extraction failed silently and Richard suggested the simpler approach. | source: this-session | proposed: enrich: tool-use-hygiene-notes
[2026-05-03T18:00 session-summary] signal: When a system has both summary columns (brand_flag, search_engine, device_tag) and a structured identifier (reftag string) that encodes the same taxonomy, the reftag is usually the source of truth and the columns are partial/lossy summaries — trying to join on the convenience columns or treating them as canonical will hit nulls and coverage gaps, while parsing the identifier string with ILIKE patterns gets you the full category system that was there all along. Caught after 4 failed join probes trying to use brand_flag/channel columns for filtering when the reftag itself was the categorization mechanism. | source: this-session | proposed: enrich: tool-use-hygiene-notes
[2026-05-03T18:10 session-summary] signal: For AB paid search, the source-of-truth split is `m_amo_paid_search` for cost/clicks/impressions but `w_ab_mktg_reftags_reg` for registrations — the reg columns on m_amo_paid_search (ref_registrations_ct, weighted_registrations_ct etc) are internal attribution variants, not the canonical credited-reg count teammates should use. Query pattern: parse reftag categories from both tables' reftag strings identically, aggregate to market×date×category grain, FULL JOIN on parsed dimensions; never try to join at row-level reftag equality because the trailing suffixes differ between tables. | source: this-session | proposed: enrich: ps-analytics-backend-options
[2026-05-03T18:30 session-summary] signal: AB Paid Search reftag taxonomy reference — Brand-Core is the primary reg driver in every mature market and should dominate the category mix; `_pv_` = product-vertical, `_cat_` = category (both NonBrand subtypes); `generic` standalone = NonBrand-Generic; `MT_` is legacy mobile-tablet and NOT a category signal; JP uses `JP_AB_PSA_<Y|G>_<BR|BRP|NonBrand>_*` where BR=Brand-Core, BRP=Brand-Pure; EU single-bucket `b2b_reg_search_branded_<market>_*` maps to Brand-Core (they don't subdivide). If your category parser is bucketing too much into "Uncategorized" or "Brand-Other", these are the corrections to apply first. | source: this-session | proposed: ps-reftag-taxonomy
[2026-05-03T19:15 session-summary] signal: AB paid search data-model quirk — cost-side marketplace_id and reg-side marketplace_id don't always match (MX spend attributes to 35691 but MX registrations appear under BOTH 35691 and 771770 because the consumer storefront handles actual registration). Always normalize via a market_map CTE (raw_id → market name like 'MX'/'US') rather than filtering queries by raw marketplace_id; otherwise MX totals will be systematically undercounted. Pattern probably repeats elsewhere with any market that has separate business/consumer storefronts. | source: this-session | proposed: ps-reftag-taxonomy
[2026-05-03T16:40 session-summary] signal: AB Paid Search URL-to-domain signal — tracking URLs pointing to consumer storefronts (www.amazon.com, www.amazon.com.mx, www.amazon.ca, www.amazon.com.au) typically carry brand campaigns landing on the consumer storefront's AB registration flow, while URLs pointing to business.amazon.* subdomains typically carry nonbrand campaigns landing on AB-specific LPs. Useful cross-check when interpreting reftag categorization: a reftag bucketed as NonBrand but hitting the consumer domain signals likely miscategorization. | source: this-session | proposed: ps-reftag-taxonomy
[2026-05-03T17:25 session-summary] signal: When shipping a SQL query library to teammates, every query must be fully self-contained and runnable as-is — no "assume these CTEs are prepended" framing, no shared-foundation references that require assembly. Teammates copy-paste to run, not to study; the 100-line repetition per derived query is the correct tradeoff against broken queries on first try. The signal for shipping a materialized view upstream is when the same CTE appears in 3+ queries and ABMA taxonomy changes would require editing N places simultaneously. | source: this-session | proposed: enrich: tool-use-hygiene-notes
[2026-05-03T18:05 session-summary] signal: Generator-provenance honesty pattern — when an agent produces a teammate-facing artifact (.docx, SharePoint doc, briefing) that references a data source the agent cannot actually query from its environment, the credits/provenance section must default to "inferred from prior-session knowledge; not verified this session" unless it can cite specific query-result artifacts with timestamps. Phrases like "iterative probing" or "verified against the cluster" are reserved for sessions where the agent actually ran the queries. Otherwise the artifact is inference wearing the clothes of verification, and teammates downstream can't tell the difference until it breaks. The failure mode repeats (happened twice in one day: Path 2 "proven cheap" and paid-search library "extensive probing"); the fix is forcing the generator script to require provenance evidence or fall back to honest defaults. | source: this-session | proposed: enrich: tool-use-hygiene-notes
[2026-05-03T18:30 session-summary] signal: Reftag token dictionary as a required up-front artifact for any URL/reftag-parsing doc — reverse-engineering CASE blocks is a teammate-adoption failure mode. | source: this-session | proposed: reftag-token-dictionary-pattern
[2026-05-03T22:50 session-summary] signal: Two agents replying to the same bus thread independently ≤3h apart don't necessarily need to reconcile — if both replies are additive layers on the same accept, thread reaches rest without a third post. | source: this-session | proposed: bus-thread-convergence-pattern
[2026-05-03T22:58 session-summary] signal: Agent-bus numeric-prefix collision when two agents reply to the same root within one window — 002_kiro-local.md + 002_kiro-server.md both exist without error; semantically fine but breaks monotonic numbering assumption. | source: this-session | proposed: bus-post-numbering-convention

[2026-05-04T01:00 session-summary] signal: data-freshness filters belong at the pipeline (single source of truth like forecast.max_week) not per-surface — file-existence ≠ data-landed, and per-consumer filters leave fabricated-headline risk from upstream synthesizers | source: this-session | proposed: enrich: dashboard-data-freshness-discipline

[2026-05-04T14:55 session-summary] signal: Hook-prompt phrasing can unintentionally route Kiro to the wrong agent type (the word "specification" was triggering spec-agent routing in am-backend.kiro.hook); worth documenting a "hook-prompt trigger words to avoid" pattern | source: this-session | proposed: enrich: kiro-hook-authoring-guide (or new: hook-prompt-antipatterns)
[2026-05-04T15:05 session-summary] signal: AM-Frontend OOO-day adaptation — when Richard is OOO, skip auto-email + calendar blocks + Asana writes, deliver single compact Tue-prep Slack DM instead. Protocol says 'execute without pausing' but 'execute' presumes Richard is the subject. | source: this-session | proposed: am-frontend-ooo-adaptation
[2026-05-05T01:46 wbr-callouts] signal: MCS LP CVR personalization stat converged at 15-20%; Richard corrected the AU 30% myth as switch-away decrease not improvement | source: main.project_timeline MCS LP decision 2026-05-01 | proposed: enrich: mcs-lp-testing-approach
[2026-05-04T18:47 session-summary] signal: ps.forecasts monthly/quarterly registration projections are unreliable model artifacts (weekly splines extrapolate incorrectly, e.g., AU M05 forecast 5,748 vs OP2 1,189) — weekly forecasts are sound but monthly/Q2 regs should not be cited without disclaimer until aggregation logic is debugged | source: this-session | proposed: enrich: ps-performance-schema (or new: forecast-aggregation-gotchas)
[2026-05-04T18:58 session-summary] signal: WBR callouts and WBR-owned state-file forecast blocks should cite Brand/NB split when ps.forecasts has segment-level metrics (brand_registrations and nb_registrations go W18-W52 across all 10 markets), not just blended 'registrations' — I defaulted to blended in the W18 run and missed the segment signal | source: this-session | proposed: enrich: ps-performance-schema (or wbr-callouts skill: surface brand/nb forecasts when available)
[2026-05-04T20:47 session-summary] signal: WBR callout paragraph 1 formula must always include ie%CCP vs target when available (MX=70% not 100%), past-tense concrete actions not future intent, YoY decomposition (clicks × CVR separately for Brand and NB) not just aggregate %, cross-channel WoW context (Unknown/Organic/Email/Direct) to frame PS performance vs channel trend | source: this-session | proposed: enrich: richard-style-wbr (paragraph 1 formula + YoY decomposition rule + cross-channel context requirement)
[2026-05-04T20:58 session-summary] signal: WW Dashboard xlsx has cross-channel (Unknown/Organic/Email/Direct) registration data by week that the dashboard-ingester doesn't extract into per-market data briefs — Richard uses this in his callouts to reframe PS WoW against channel trend (e.g., MX W18 "PS +22% while Unknown -29%" or AU W18 "PS -7% while most channels -20-30%"); the ingester should add a cross-channel section to each data brief | source: this-session | proposed: enrich: dashboard-ingester-spec (or new: wbr-cross-channel-context-rule)

[2026-05-04T21:42 eod] signal: WBR callout paragraph 1 needs clicks×CVR decomposition + headline ie%CCP, not future-intent YoY aggregates | source: session-log 2026-05-04 voice rewrite + richard-style-wbr.md patch | proposed: enrich: richard-style-wbr
[2026-05-05T14:40 session-summary] signal: When an aim-launched MCP goes silent, the first-line diagnostic is running the aim mcp start-server command directly from shell — it surfaces the real startup error (usually MIDWAY_AUTHENTICATION_ERROR from 0-byte cookie) that Kiro hides as "tool not available"; pre-flight cookie check in AM-Backend Phase 0 would catch auth expiry before it silently degrades downstream hooks. | source: this-session | proposed: enrich: mcp-auth-failure-playbook
[2026-05-05T14:48 session-summary] signal: Two distinct MCP auth failure modes in one session — (a) aim-launched MCPs (Slack, SharePoint) need mwinit -f AND brazil-package-cache daemon restart because the daemon caches auth state at startup, (b) mcp-remote MCPs (Hedy) can be manually re-authed by reading the existing PKCE verifier from ~/.mcp-auth, building the authorize URL yourself, and posting the returned code to /oauth/token — the container-vs-Mac localhost split means the default browser-based flow never completes. | source: this-session | proposed: enrich: mcp-auth-failure-playbook

[2026-05-05T14:59 session-summary] signal: Kiro MCP clients (Slack, SharePoint) hold stale auth tokens across mwinit -f + brazil-package-cache restart — requires explicit panel-reconnect or window-reload — 3rd recurrence in session-log (4/22, 4/25, 5/5), now qualifies as recurring friction that pre-flight checks in am-backend Phase 0 should catch | source: this-session | proposed: midway-auth-recovery-protocol

[2026-05-05T15:03 session-summary] signal: "degraded" is a load-bearing word — running a pipeline with 4/5 inputs dead produces state files that are not "partial" but "actively misleading" because the output schema encodes freshness claims the inputs can't back; refusing to run is the right call even when the user says "run" if the output would lie about its own provenance | source: this-session | proposed: enrich: midway-auth-recovery-protocol

[2026-05-05T15:12 session-summary] signal: Midway cookie can be dead at server while still looking fresh client-side (40min old, 1004 bytes, previously worked) — `curl --cookie ~/.midway/cookie https://amazon.sharepoint.com` is the 1-command diagnostic that isolates cookie-invalid from Kiro-client-stale, and should be the first check not the last when MCPs 401 after a supposed fix | source: this-session | proposed: enrich: midway-auth-recovery-protocol

[2026-05-05T15:18 session-summary] signal: "Reconnect MCPs in Kiro panel" and "refresh midway cookie" are two independent fixes for different failure modes — panel reconnect only helps if server processes crashed/hung, cookie refresh only helps if auth token expired at server; conflating them wastes turns because the curl-cookie-test disambiguates in one command | source: this-session | proposed: enrich: midway-auth-recovery-protocol

[2026-05-05T17:30 session-summary] signal: "Refusing to run" felt like discipline but was the wrong read of the protocols — am-backend-parallel.md failure table and topic-sentry.md step 3 explicitly define "partial output with per-source staleness flags" as correct behavior when inputs are dead; the failure mode to avoid is silent degradation (state file labeled 'fresh' that isn't), not partial execution itself; took 3 turns + explicit "keep iterating" to see the framing error | source: this-session | proposed: enrich: midway-auth-recovery-protocol
[2026-05-05T20:45 eod] signal: MCP auth dead all day despite mwinit -f refresh; same-cookie-different-state (CLI smoke tests passed 14:37 UTC, HTTP 401 sustained through 20:45 PT). Refresh pattern may need second-factor or network-event trigger. | source: session-log 2026-05-05 entries | proposed: enrich: midway-cookie-silent-expiry-note
