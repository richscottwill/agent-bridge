# Topic Watchlist — Sentry Proposals

_Generated by Broad Sweep 2026-04-29 (YTD pass). Apply manually in `~/shared/context/body/topic-watchlist.md`. Proposals do not self-execute._

## ADD

### op1-forecast-flat-budget (P2, operational)

Proposed new topic. Emerging cluster in `signals.signal_tracker` with strength 2.0 at 2026-04-29 in `om-team` channel: "OP1 forecast: flat YoY budget. Break out Small SMB. Reforecast RoY 2026." Hits OP1/OP2 planning cycles that neither `mx-budget-ieccp` (MX-scoped) nor `ww-testing-methodology` (methodology-scoped) cover.

```
### op1-forecast-flat-budget
- **Status:** active
- **Priority:** P2
- **Level:** operational
- **Keywords:** "OP1 forecast", "flat YoY budget", "SMB breakout", "RoY reforecast", "2027 OP1", "OP1 inputs", "OP2 reforecast", "NA MCC budget", "Paid Acquisition OP1"
- **Senders/distros:** brandoxy@amazon.com, dconejr@amazon.com (David Cone), kataxt@amazon.com
- **Channels:** email, slack, hedy, loop
- **Why it matters:** OP1/OP2 cycle planning drives budget envelope for all 10 markets. Current YTD `signal_tracker` already has 4 `op1-strategy` reinforcements in Hedy; adding explicit watchlist entry lets daily Sentry surface these alongside `mx-budget-ieccp` rather than relying on tracker surfacing.
- **Review:** 2026-07-31
```

## FOLD-IN (no new topic)

### pam-budget-availability → extend mx-budget-ieccp

Strength 3.6 in `ab-paid-search-app`, 10-day unanswered, MX-scoped. Rather than a new topic, extend `mx-budget-ieccp` keywords to cover PAM-related phrasings:

Add to existing keywords: `"PAM availability"`, `"PAM bandwidth"`, `"PAM offsite"`, `"PAM budget"`, `"PAM availability confirmed"`.

## DEMOTE

### competitor-intel: P3 → monitoring

0 YTD hits across all sources (476 emails + 830 Slack messages + 15 Hedy meetings, 4-month window). Keep the topic on the audit trail — competitor intel is inherently bursty — but downgrade daily surfacing.

**Edit:** Change `Priority: P3` to `Priority: P3 (monitoring)`, or move the entire block from the P3 section to the Monitoring section in `topic-watchlist.md`. Review date unchanged (2026-10-31).

## NO ACTION — review confirmed

### new-markets: already monitoring

0 YTD hits confirms correct placement. Review 2026-10-31 holds.

### kiro-agentspaces-tooling: stay P2

115 YTD hits but volume is dominated by external community Q&A in `agentspaces-interest` and `amazon-builder-genai-power-users`. Hit count would justify P1 purely numerically, but P1 inflation risk outweighs signal-to-action ratio. Keep at P2.

## KEYWORD TUNE

### mx-registration-funnel — widen keyword set

Only 2 YTD hits despite active workstream (Cristobal 4/24 launch landing on `sparkle-and-baloo` tracker instead). Proposed keyword additions:

- `Cristobal`
- `Cris launch`
- `AVP shift`
- `verification uplift`
- `Mexico verification`
- `MX AVP`

Keep existing keywords.

## Data quality flags (for maintenance)

1. `ops.sync_watermarks` table does not exist. Create with schema: `(source VARCHAR PRIMARY KEY, last_run_at TIMESTAMP, sources_ingested JSON)` before next sweep.
2. `docs.loop_pages.last_modified` is null for all 5 pages. Populate on ingestion so Sweep can detect drift.
3. AM-Backend email ingestion horizon: earliest row 2026-03-19. If 90-day or YTD audits matter, backfill required.
4. AM-Backend Hedy ingestion horizon: earliest row 2026-04-14. Similar backfill gap.
5. `signals.slack_messages.channel_section` missing (flagged in topic-sentry.md today). Implement when schema is revised.
