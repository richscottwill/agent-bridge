# ⚠️ DEPRECATED — Do Not Use

This directory is deprecated as of 2026-03-25.

Published articles now live in `~/shared/artifacts/{category}/` using the artifact naming convention (`YYYY-MM-DD-short-description.md`) and artifact front-matter standard.

The wiki pipeline (research → staging → review) stays in `~/shared/context/wiki/`.
The wiki-index.md indexes articles in `~/shared/artifacts/`.

See:
- `~/shared/context/wiki/wiki-structure.md` for the publishing workflow
- `~/shared/context/wiki/wiki-index.md` for the article index
- `~/shared/artifacts/README.md` for the artifact standard
