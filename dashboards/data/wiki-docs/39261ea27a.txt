# Wiki Health Report — 2026-05-01 (W18)

Generated by the weekly wiki-maintenance hook. First run against the new
`AB-Paid-Acq-*` SharePoint layout adopted 2026-04-28 (replaces the old
`Artifacts/<category>/` tree).

## Summary counts

| Metric | Value | Source |
|---|---|---|
| Total indexed articles | 311 (+216 grouped, 51 archived) | `build-wiki-index.py` stdout |
| Live `agent-created/` articles (excl. reviews/archive/_meta/intake/kiro-steering) | 97 | `find` count |
| Orphans | 0 | index count vs filesystem count |
| Broken relative `.md` cross-refs | 0 | local link-resolver scan |
| Articles missing required frontmatter (title/status/audience) | 24 | frontmatter scan |
| SharePoint published | 48 | `sharepoint-artifacts.json` → builder |
| SharePoint local-only | 59 | same |
| SharePoint published-stale (local newer than SP) | 1 | same |

## doc-type distribution (new 4-bucket vocab, applied 2026-05-01)

| doc-type | Count |
|---|---|
| `strategy` | 8 |
| `reference` | 7 |
| `execution` *(legacy, pending retag → strategy/tactical)* | 9 |
| `tactical` | 0 |
| `snapshot` | 0 |
| *(unset)* | 73 |

**Observation:** the 4-bucket vocabulary shipped today but 73 of 97 live articles
still carry no `doc-type`. This is the tail of yesterday's retag work — the
vocabulary patch landed at the pipeline-rules level and on a handful of newer
articles, but a sweep across the corpus hasn't run yet. Decay alerts can't fire
on unset buckets, so decay-audit is effectively dark for 75% of the corpus until
retag lands.

## Decay-audit results (bucket-aware, today)

| Bucket | Rule | Hits |
|---|---|---|
| `tactical` | updated >90d | 0 (no tagged tactical articles yet) |
| `reference` | updated >540d + no `foundational:true` | 0 |
| `strategy` | updated >3y + no `review_due` | 0 |
| *(unset)* | legacy >90d rule until retag | 0 (corpus freshly touched 4/28 during folder reorg) |

Everything looks healthy because the 4/28 SharePoint reorg bumped modified-dates
on nearly the whole corpus, masking real age. Next week's audit will be the first
honest staleness read.

## SharePoint crawl

Ran against the new three-folder layout:

- `AB-Paid-Acq-Team/programs/{oci, polaris, ai-max, aeo-ai-overviews, ieccp-planning, agentic-ps, op1-testing-framework}/**`
- `AB-Paid-Acq-Team/methodology/{kiro-for-team, testing, handoffs, callouts, reporting}/**`
- `AB-Paid-Acq-Team/library/{daily-briefs, ad-copy-results, competitor-intel, meeting-briefs, research-clips}/**`
- `AB-Paid-Acq-Team/markets/**` — 12 market stubs, no content yet (v2 layout created empty)
- `AB-Paid-Acq-Dashboards/` — top-level xlsx only
- `AB-Paid-Acq-Ops/{audit-logs, agent-state, tools, portable-body}/**`
- `.Richard-Private/**` — intentionally excluded (coaching/career/drafts)

89 `.md`/`.docx`/`.xlsx` artifacts cached. Compare against 87 in the v1 `Artifacts/`
tree from 2026-04-17 — net +2 as expected (recent polaris MX NB LP test + Fortune
text audit). No artifacts lost in the reorg.

## Follow-ups for next week

- **4-bucket retag sweep** — 73 articles need `doc-type:` assignment. Deferred
  to the next wiki-critic audit mode run (per the applied vocabulary doc). Until
  that runs, decay audit is effectively noop.
- **Legacy `execution` retag** — 9 articles still carry the old value; map
  per heuristic in wiki-pipeline-rules.md §9 (playbook → reference, test-* → tactical,
  everything else → strategy).
- **24 articles missing required frontmatter** — same sweep should fix these.
- **SharePoint-stale × 1** — one local article is newer than its SharePoint
  copy. The `sharepoint-sync` hook should catch this on next Kiro-Drive push.
- **`markets/` folder empty on SharePoint** — 12 stub folders with no content.
  Either seed state files or remove the stubs; current state false-positives the
  dashboard as "markets not published."

## What changed in the maintenance tooling this run

- Added `~/shared/dashboards/refresh-sharepoint-cache.py` — reads folder
  listings from stdin JSON (one SharePoint MCP call per top-level folder) and
  writes `data/sharepoint-artifacts.json` in the v2 schema the wiki-index
  builder already consumes. Skips `_index.md` / `README.md` as structural
  rather than canonical articles.
- Updated `shared/.kiro/hooks/wiki-maintenance.kiro.hook` Phase 2 to crawl the
  new folder set (see hook file for the full folder list).
