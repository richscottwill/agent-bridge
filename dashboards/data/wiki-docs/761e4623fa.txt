# Wiki Audit — 2026-05-01 (W18)

Weekly lint audit. Paired with `health-2026-05-01.md`.

## Findings

### 1. Orphan scan — PASS

`find` count: 97 live articles. `wiki-index.md` summary Total: 105 (includes
reviews + _meta). No orphans — the builder's filesystem scan is the source of
truth and everything on disk is represented.

### 2. Stale content — PASS (with asterisk)

Per the new 4-bucket rules (wiki-pipeline-rules.md §9):

- 0 tactical docs past 90 days
- 0 reference docs past 18 months
- 0 strategy docs past 3 years without `review_due`

**Asterisk:** 73 articles have no `doc-type` assigned, so decay rules don't
apply to them yet. Additionally, nearly the whole corpus got its modification
date bumped during the 4/28 SharePoint reorg, so trailing-age queries look
cleaner than they are. Real staleness will surface next week once the 4/28 bump
drops out of the recency window.

### 3. Broken cross-refs — PASS

Scanned relative `.md` links across the 97 live articles. 0 unresolved targets.

### 4. Missing frontmatter — 24 articles

Required fields per wiki-pipeline-rules.md: `title`, `status`, `audience`,
`owner`. 24 articles fail at least one. Largely correlated with the 73
unset-doc-type articles — next retag pass should pick these up.

### 5. SharePoint-stale — 1 article

One article is newer locally than on SharePoint. Will be picked up by the
next `sharepoint-sync` run. No action required.

### 6. Local REVIEW + unpublished — 48 articles

48 articles carry `status: REVIEW` but aren't on SharePoint. This is expected
mid-pipeline state under the old status vocab. The new prompt-only constraint
protocol (wiki-pipeline-rules.md §8, shipped today) flips the REVIEW→FINAL
gate onto the librarian, so these won't move until the wiki pipeline actually
runs end-to-end — which hasn't happened since 2026-04-05 per the blackboard
kill review findings earlier today.

**Not an action item this week** — the bottleneck is wiki-writer/critic
invocation cadence, not article-readiness. Track whether the first post-kill
pipeline run clears any of these. If none clear by 2026-05-15, revisit whether
the REVIEW stack itself is the problem.

### 7. Signal-based freshness — `signals.wiki_candidates` view

Top 6 topics by quality score from multi-channel signal aggregation:

| Topic | Quality | Mentions | Channels | Most recent |
|---|---|---|---|---|
| polaris-brand-lp | 40.6 | 14 | 6 | 2026-04-29 |
| oci-rollout | 19.6 | 5 | 3 | 2026-04-29 |
| au-cpa-cvr | 10.8 | 3 | 3 | 2026-04-20 |
| mx-budget-ieccp | 10.4 | 3 | 2 | 2026-04-22 |
| op1-forecast-flat-budget | 6.9 | 2 | 2 | 2026-05-01 |
| au-transition | 6.9 | 2 | 2 | 2026-05-01 |

Cross-referenced against existing wiki-index:
- `polaris-brand-lp` → no dedicated article; closest is `polaris-program`.
  Strong signal, worth its own article or a deep subsection.
- `oci-rollout` → covered by `oci-program`. No new article needed; rollout status
  may have drifted in 2 weeks, worth a state check.
- `au-cpa-cvr` → covered by `au-market-wiki` but section may need enrichment.
- `mx-budget-ieccp` → crosses `mx-market-wiki` + `ieccp-planning-framework`;
  needs a cross-link rather than a new article.
- `op1-forecast-flat-budget` → new, only 2 mentions so far. Wait one more week
  for signal confirmation.
- `au-transition` → new, 2 mentions. Hands-off AU handoff already has its own
  doc — verify whether this signal is about handoff status or new work.

### 8. Blackboard health check — N/A (killed)

Blackboard experiment was killed today (wiki-pipeline-rules.md §7 retired).
Replacement is prompt-only constraint handoff in §8. No sidecar files to count.

## Protocol-level findings this run

**v2 SharePoint layout adopted.** The old `Artifacts/<category>/` tree has
been replaced by the three `AB-Paid-Acq-*` folders (plus `.Richard-Private/`,
out of scope for the wiki pipeline). This run is the first to execute against
the new layout. The hook protocol, cache refresh mechanism, and dashboard
index builder were all updated in-flight:

- New: `~/shared/dashboards/refresh-sharepoint-cache.py` — builds the
  SharePoint cache JSON from MCP folder listings, with README/_index skip
  rules, dedup-by-newest-mtime, and a `layout_version: 2` breadcrumb.
- Updated: Phase 2 of the wiki-maintenance hook now points at the new
  folder list.
- Preserved: the index builder's `sharepoint-artifacts.json` consumer
  didn't need changes — schema is the same, only the folder set that
  fills it changed.

**Next week should be the first "clean" run** with proper week-over-week
deltas. This week's run establishes the v2 baseline.
